---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledge-graph
  namespace: jujube
  labels:
    app: knowledge-graph
    app.kubernetes.io/name: jujube-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: knowledge-graph
  template:
    metadata:
      labels:
        app: knowledge-graph
    spec:
      containers:
        - name: knowledge-graph
          image: jujube/knowledge-graph:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: jujube-config
            - secretRef:
                name: jujube-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
